pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "SwiftLibSample"
include(":androidApp")
include(":shared")
include(":CryptoKitWrapper")